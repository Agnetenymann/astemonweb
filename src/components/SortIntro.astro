---
import "../styles/generel.css";
---

<section class="hero" id="sorting-guide">
  <div class="hero__inner">
    <div class="grid_1-1">
      <div class="hero__text">
        <h2>HOW TO SORT YOUR WASTE LIKE A PRO</h2>
        <h3>Not sure what goes where? Don’t worry — we’ve got you!</h3>

        <p>
          Sorting waste in a new city can be confusing, but it doesn’t have to
          be. This guide gives you a quick and easy overview of how to sort the
          most common types of waste in Lisbon — from glass bottles to paper,
          packaging, and organics.
        </p>

        <p>
          Perfect for you who want to keep the city clean and do your part for a
          more sustainable Lisbon (no dictionary needed!).
        </p>
      </div>
    </div>
  </div>
  <div class="hero__globe">
    <img src="/img/globe.svg" alt="" />
  </div>
</section>

<style>
  .hero {
    background-color: var(--light_green);
    border-bottom: 0.5px solid var(--ashbrown);
  }

  .grid_1-1 {
    max-width: 900px;
    margin: 0 auto;
    padding: 10rem 0rem 3rem;
    gap: 3rem;
    align-items: center;
    display: grid;
    grid-template-columns: 4fr 1fr;
    align-items: center;
  }

  .line {
    height: 0.5px;
    background-color: var(--ashbrown);
    margin: 0 auto;
    width: 100%;
  }

  .hero__inner2 {
    max-width: 900px;
    margin: 0 auto;
    padding: 4rem 0rem 3rem;
    margin-top: -2rem;
    gap: 3rem;
    align-items: center;
  }

  .hero__globe {
    display: block;
    position: relative;
    max-width: 280px;
    height: auto;
    margin-bottom: 2rem;
  }

  .hero__text {
    z-index: 1;
  }

  .hero__sub {
    font-size: 0.85rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    margin-bottom: 1.5rem;
    color: var(--ashbrown);
  }

  .hero__lets-sort {
    position: relative;
    display: flex;
    gap: 4rem;
  }

  .hero__links {
    list-style: none;
    margin: 0;
    padding: 0;
    font-size: 0.9rem;
    display: flex;
    flex-direction: column;
    gap: 0.55rem;
  }

  @media (max-width: 1100px) {
    .hero__inner {
      gap: 2.5rem;
    }
  }

  @media (max-width: 800px) {
    .hero__inner {
      gap: 2.5rem;
      padding: 6rem 3rem 3rem;
    }

    .hero__inner2 {
      gap: 2.5rem;
      padding: 1rem 3rem 3rem;
    }
    .hero__lets-sort {
      gap: 1.5rem;
    }

    .hero__globe {
      display: none;
    }

    .grid_1-1 {
      display: grid;
      grid-template-columns: 1fr;
      gap: 0.2rem;
      align-items: center;
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 0;
      margin-left: 0;
      margin-top: -6rem;
    }
  }
</style>
