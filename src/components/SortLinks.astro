---
import BoxButton from "./BoxButton.astro";
import "../styles/generel.css";
---

<section class="external-links" id="external-links">
  <div class="external-links__inner">
    <div class="title-external-links">
      <span>EXTER-</span>
      <span>NAL</span>
      <span>LINKS</span>
    </div>
    <div class="external-links__rows">
      <div class="external-links__row external-links__row--top">
        <p>
          More waste that you don’t know hoe to dispose of? Check out this
          website for more specific items, and how to dispose of them
        </p>

        <BoxButton buttontext="WASTE APP" buttonurl="/cycle" maxWidth="auto" />
      </div>

      <div class="external-links__row external-links__row--bottom">
        <p>
          Are you not sure where you can dispose of your different types of
          waste? Don’t worry! Just have a look here and learn where you can find
          the nearest drop off point
        </p>

        <BoxButton buttontext="ECOPONTO" buttonurl="#" maxWidth="auto" />
      </div>
    </div>
  </div>
</section>

<style>
  .external-links {
    background-color: var(--light_green);
  }

  .external-links__inner {
    max-width: 900px;
    margin: 0 auto;
    padding-top: 3rem;
    padding-bottom: 3rem;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    column-gap: 2rem;
  }

  .title-external-links {
    font-weight: 500;
    font-size: 2.8rem;
    font-family:
      Lexend giga,
      sans-serif;
    line-height: 1.3;
    color: #4b4444;
    align-self: flex-start;
  }

  .title-external-links span {
    display: block;
  }

  /* RIGHT COLUMN */
  .external-links__rows {
    display: flex;
    flex-direction: column;
  }

  .external-links__row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    margin-bottom: 1.5rem;
    margin-top: 0.6rem;
  }

  .external-links__row p {
    margin: 0;
    line-height: 1.6;
    font-size: 0.97rem;
    max-width: 28rem;
    color: #514a4a;
  }

  @media (max-width: 1050px) {
    .external-links__inner {
      grid-template-columns: 1fr;
      padding: 3rem;
    }

    .title-external-links {
      font-size: 2.2rem;
    }

    .external-links__row {
      gap: 1rem;
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
